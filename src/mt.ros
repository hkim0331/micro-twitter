#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  ;;#+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.mt.3764407911
  (:use :cl))
(in-package :ros.script.mt.3764407911)
(ql:quickload :mt)

(defun main (&rest argv)
  (declare (ignorable argv))
  (in-package :mt)
  (start-server))
;;; vim: set ft=lisp lisp:
